<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subtle Meme News</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
</head>

<body>

    <header>
        <nav>
            <div class="nav-wrapper blue lighten-1">

                <a class="brand-logo" href="/">Subtle Meme News</a>


                {{#if loggedIn}}
                <a class="right" href="/dashboard">dashboard</a>
                <button class="waves-effect waves-light btn right" id="logout">logout</button>
                {{else}}
                <a class="waves-effect waves-light btn right" href="/login">login</a>
                {{/if}}
            </div>
        </nav>

    </header>
    <main>
        {{{ body }}}
    </main>
    <footer>
        Thanks for visiting!
    </footer>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>